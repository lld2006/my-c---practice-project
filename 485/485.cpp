#include "tools.h"
int main()
{
    vector<int> vprimes;
    primeWithin(vprimes, primeLimit);
    i64 ntotal = 0;
    for(unsigned int i = 0; i < maxBlocks; ++i){
        getSum(nstart, nend, nsegement, vprimes);
    }
}
