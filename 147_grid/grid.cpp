//this problem should be easy using dynamic programming.
//the difficult part is to find the cross hatched one's boundary
//
#include <cstdio>
#include "../lib/tools.h"
int xb=3;
int yb=2;
int main(){
    vector<i64> normal;
    vector<i64> cross;
    normal.resize((xb+1)*(yb+1), 0);
    cross.resize((2*xb+1)*(2*yb+1), 0);
    for(int x = 1; x<=xb; ++x)
        for(int y = 1; y<=yb; ++y){

        }
}
